import React from 'react';
import {render} from 'ink';

import {App} from './app.tsx';

/** Point d'entrée du TUI MasterFlow. Lancement : `npm run dev:tui` (terminal interactif requis). */
render(<App />);
