import{beforeAll,describe,it,expect}from'vitest';import{getDb}from'../src/db/schema.ts';import{seedAll}from'../src/db/seed.ts';import type{AuthUser}from'../src/middleware/auth.ts';import{createPrivateQuoteDraft,listPrivateQuoteDrafts}from'../src/services/private_quotes.ts';const owner:AuthUser={id:'quote-owner',username:'quote_owner',role:'teacher'};beforeAll(async()=>{await seedAll();const n=Date.now();getDb().prepare("INSERT OR IGNORE INTO users(id,username,display_name,password_hash,role,active,created_at,updated_at)VALUES(?,?,?,'x',?,1,?,?)").run(owner.id,owner.username,owner.username,owner.role,n,n);});describe('private quote',()=>{it('requires price sources and computes a private total',()=>{const q=createPrivateQuoteDraft(owner,{client_label:'Client test',currency:'eur',lines:[{label:'Atelier',quantity:2,unit_price:450,price_source_ref:'price://catalog/atelier',confidence:'high'}],assumptions:['Deux sessions'],exclusions:['Transport'],validity:'30 jours'});expect(q).toMatchObject({currency:'EUR',total:900,status:'draft'});expect(listPrivateQuoteDrafts(owner)).toHaveLength(1);expect(Object.keys(q)).not.toContain('send_status');});});
