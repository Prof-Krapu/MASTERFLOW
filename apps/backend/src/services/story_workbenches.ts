import {CreateStoryWorkbenchRequestSchema,ROLE_RANK,SetStoryReaderStateRequestSchema,StoryReaderStateSchema,StoryWorkbenchSchema,type CreateStoryWorkbenchRequest,type SetStoryReaderStateRequest,type StoryReaderState,type StoryWorkbench} from '@masterflow/shared';
import {getDb} from '../db/schema.ts';import {uuid} from '../lib/uuid.ts';import type {AuthUser} from '../middleware/auth.ts';
function teacher(a:AuthUser):void{if(ROLE_RANK[a.role]<ROLE_RANK.teacher)throw new Error('permission_denied');}
function workbench(r:any):StoryWorkbench{return StoryWorkbenchSchema.parse({workbench_id:r.id,owner_id:r.owner_id,project_id:r.project_id,project_scope:r.project_scope,title:r.title,source_ref:r.source_ref,intake_mode:r.intake_mode,source_truth_state:r.source_truth_state,status:r.status,created_by:r.created_by,created_at:r.created_at,updated_at:r.updated_at});}
function state(r:any):StoryReaderState{return StoryReaderStateSchema.parse({reader_state_id:r.id,workbench_id:r.workbench_id,owner_id:r.owner_id,current_node:r.current_node,opening_sequence_lock:r.opening_sequence_lock,mode:r.mode,created_at:r.created_at,updated_at:r.updated_at});}
export function createStoryWorkbench(a:AuthUser,input:CreateStoryWorkbenchRequest):StoryWorkbench{teacher(a);const q=CreateStoryWorkbenchRequestSchema.parse(input),now=Date.now(),id=uuid(),scope=q.project_id??a.id;getDb().prepare("INSERT INTO story_workbenches VALUES (?,?,?,?,?,?,?,?,'draft',?,?,?)").run(id,a.id,q.project_id??null,scope,q.title,q.source_ref,q.intake_mode,q.source_truth_state,a.id,now,now);return workbench(getDb().prepare('SELECT * FROM story_workbenches WHERE id=?').get(id));}
export function setStoryReaderState(a:AuthUser,id:string,input:SetStoryReaderStateRequest):StoryReaderState{teacher(a);const w=getDb().prepare('SELECT * FROM story_workbenches WHERE id=? AND owner_id=?').get(id,a.id)as any;if(!w)throw new Error('story_workbench_not_found');const q=SetStoryReaderStateRequestSchema.parse(input),now=Date.now(),existing=getDb().prepare('SELECT * FROM story_reader_states WHERE workbench_id=? AND owner_id=?').get(id,a.id)as any;if(existing){getDb().prepare('UPDATE story_reader_states SET current_node=?,opening_sequence_lock=?,mode=?,updated_at=? WHERE id=?').run(q.current_node??null,q.opening_sequence_lock??null,q.mode,now,existing.id);return state(getDb().prepare('SELECT * FROM story_reader_states WHERE id=?').get(existing.id));}const sid=uuid();getDb().prepare('INSERT INTO story_reader_states VALUES (?,?,?,?,?,?,?,?)').run(sid,id,a.id,q.current_node??null,q.opening_sequence_lock??null,q.mode,now,now);return state(getDb().prepare('SELECT * FROM story_reader_states WHERE id=?').get(sid));}
